apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: flux-system
  namespace: ${nameSpace}
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: git-read-token
  url: https://github.com/{GitHubOrg}/${GitHubRepo}.git