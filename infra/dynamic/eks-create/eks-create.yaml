apiVersion: infra.contrib.fluxcd.io/v1alpha1
kind: Terraform
metadata:
  name: '${resourceName}-eks-${resourceName}-${clusterName}'
  namespace: ${templateNamespace}
spec:
  interval: 10m
  retryInterval: 20s
  path: ./terraform/cluster-templates/eks-create
  approvePlan: auto
  destroyResourcesOnDeletion: true
  writeOutputsToSecret:
    name: '${resourceName}-eks-${resourceName}-${clusterName}-config'
  dependsOn:
    - name: '${resourceName}-vpc-${vpcResource}-${vpcName}'
  backendConfig:
    customConfiguration: |
      backend "s3" {
        key            = "${clusterName}/${templateNamespace}/${resourceName}/eks/${resourceName}/${clusterName}/terraform.tfstate"
        bucket         = "${prefixName}-${awsAccountId}-${awsRegion}-tf-state"
        region         = "${awsRegion}"
        encrypt        = true
        dynamodb_table = "${prefixName}-${awsAccountId}-${awsRegion}-tf-state"
      }
  vars:
    - name: cluster_name
      value: ${resourceName}-${clusterName}
    - name: eks_version
      value: ${clusterVersion}
    - name: region
      value: ${awsRegion}
  varsFrom:
    - kind: Secret
      name: ${resourceName}-vpc-${vpcResource}-${vpcName}-config
      varsKeys:
        - vpc_id
        - private_subnets_string
        - public_subnets_string
        - vpc_cidr
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  runnerPodTemplate:
    spec:
      envFrom:
      - secretRef:
          name: aws-creds